# Portfolio Backend Environment Variables

# Email configuration for contact form
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-app-specific-password

# Where to receive contact form submissions (defaults to EMAIL_USER if not set)
CONTACT_EMAIL=your-email@gmail.com

# Server configuration
PORT=5000
NODE_ENV=development

# Instructions:
# 1. Copy this file to .env
# 2. Replace the placeholder values with your actual email credentials
# 3. For Gmail, you'll need to use an "App Password" instead of your regular password
#    - Go to your Google Account settings
#    - Security > 2-Step Verification > App passwords
#    - Generate a new app password for this application
# 4. Never commit the .env file to version control